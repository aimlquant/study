# ML4T 3판 1장 리포트 — Claude R3 최종 판정

- 검토자: Claude (이 문서만 소유)
- 검토일: 2026-09-06
- 판정 대상: `review-baseline-r3.json` 59개 파일. 검토 시작·종료 시점에 **59개 전부 기준선 해시와 동일** (변경 0, 누락 0)
- R2 대비 바뀐 파일: 직접 해시 대조 결과 **정확히 4개** — 공용 템플릿과 회차의 `report.css`·`report.js`.
  `report.html`, 그림 10개, `content.json`, `presentation.toml`, 원자료 해시는 R2와 **바이트 동일**
- 시각 판정 근거: `tmp/browser-shots/ml4t-ch01-r3-20260906/qa-ready.json` (`status: ready`, 13:38:43 KST).
  기록된 소스 59개와 출력 3개의 SHA-256을 전수 재계산해 **전부 일치**

## 남은 세 건: 모두 종결

### 1. 확대 보기 제목 — 종결

`getLabel()` 이 칩과 제목을 개별 선택해 가운데점으로 잇도록 바뀌었고, 선택자 목록의 문서 순서 매칭 문제가 사라졌습니다.
`figcaption` 폴백은 별도 분기로 남아 실제로 도달 가능해졌습니다. 회차와 공용 템플릿의 `report.js` 는 **전체가 바이트 동일** 합니다.

- 데스크톱 렌더 확인: 툴바 제목이 `그림 2 · 공유 데이터 기반과 탐색·확인의 경계`. R2의 붙어 나오던 표기가 해소됐습니다
- 8개 상호작용 항목이 데스크톱·모바일 화면 모두에서 `PASS` 로 표시됩니다(목차 열림, 실습 목차 등재, 링크 닫힘, 그림 10개 키보드 접근, Enter 확대, 배율 변경, Esc 포커스 복귀, 클릭 확대)
- 참고: `mobile-interaction.png` 는 R2와 해시가 같습니다. 모바일 툴바는 제목을 표시하지 않는 설계라 이번 변경이 픽셀에 나타나지 않는 것이며, 결함이 아닙니다.
  **제목 수정 자체의 화면 증거는 데스크톱 렌더입니다.**

### 2. 인쇄 — 종결

37쪽에서 34쪽으로 줄었습니다. 34쪽 전부를 페이지별 세로 사용률로 계량하고 해당 페이지를 원본 크기로 열람했습니다.

| 항목 | R2 | R3 |
| --- | --- | --- |
| 출처 문장만 남은 페이지 | 8쪽(5.5%) · 20쪽(2.8%) | 없음 |
| 도입의 외톨이 문단 | 3쪽(7.9%) | 없음 — 2쪽에 제목·요약 상자·본문·표 1이 함께 (95.4%) |
| 절 시작과 그림의 분리 | 9쪽(37.6%)에서 그림이 다음 쪽으로 밀림 | 없음 — 7쪽에 §1.2 제목·요약 상자·본문·그림 2가 함께 (95.4%) |
| GMM 코드 분할 | 27\~28쪽에 걸쳐 4줄만 남고 갈라짐 | 없음 — 24쪽에 그림 8과 전체 코드가 온전히 |
| 마지막 쪽 제외 최저 사용률 | 2.8% | 18.2% |

원인 규칙이 정확히 손질됐습니다. 인쇄 계층에 `.source-note { break-before: avoid-page; }` 와
`.report-section pre { break-inside: avoid-page; }` 가 들어가고 절의 인쇄 상하 여백이 0이 됐습니다. 공용 템플릿에도 같은 블록이 있습니다.

남은 짧은 페이지 3건(6·13·26쪽, 18\~33%)은 열어 보니 전부 **절 마지막의 해석·한계 문단 + 실습 근거 + 출처가 한 덩어리로 놓인 절 경계 페이지** 입니다.
절의 `break-before: page` 를 유지한 결과이며, R2에서 지적한 "출처 한 줄만 남는" 유형과 성격이 다릅니다. 요청문의 설명과 일치하므로 결함으로 세지 않습니다.
마지막 34쪽(36.4%)은 문서 끝입니다.

PDF 텍스트를 다시 추출해 **원시 LaTeX 토큰 0개, 유니코드 아래첨자 0개, 셀 해시 0개** 이며 19쪽의 표준화·GMM 수식이 정상 조판됨을 확인했습니다.
코드 블록 두 개 모두 가로 잘림과 스크롤바가 없습니다.

### 3. 최종 바이트 뒤 검증 — 종결

R2에서 지적한 순서 역전이 해소됐습니다. 변경된 4개 파일의 최종 수정은 13:35:27이고 다섯 로그는 모두 그 뒤입니다.

| 증거 | 시각 | 결과 |
| --- | --- | --- |
| `tests-r3.log` | 13:35:33 | 282개 `OK (skipped=5)` |
| `build-r3.log` | 13:35:33 | `generated site already up to date` · `generated site is up to date` |
| `validation-r3.log` | 13:35:34 | 95개 HTML 통과, 경고 22건은 기존 외부 iframe |
| `diff-check-r3.log` | 13:35:34 | 출력 없음(`git diff --check` 통과) |
| `skill-r3.log` | 13:35:34 | `Skill is valid!` |

건너뛴 5개는 `httpx`·`neo4j`·`openai` 선택 의존성 테스트로 교재와 무관합니다.
**`test_source_fidelity` 36개는 전부 실행됐고 건너뛴 것이 0개** 이므로, 교재 심링크가 없을 때 조용히 넘어가는 상태가 아니었음을 확인했습니다.
13:35:34 이후 변경된 파일은 Codex 자체 원장 하나뿐이며 판정 대상이 아닙니다.

## 여섯 게이트 최종 판정

| 게이트 | 판정 | 근거 |
| --- | --- | --- |
| 1. 원본 구조 충실성 | 충분 | `report.html` 과 그림 10개가 R2와 바이트 동일. R2에서 원서 그림 1.1 과업 6개까지 대조 완료 |
| 2. 의미와 주장 충실성 | 충분 | 내용 파일이 바뀌지 않았고 `content.json` 원자료 해시도 동일. 인쇄 추출 재확인에서도 수치·기호 변형 없음 |
| 3. 독립 문서 완결성 | 충분 | 동일성 유지. PDF 추출에서 셀 해시 0건 재확인 |
| 4. 학습성과 설명성 | 충분 | 34쪽 추출에서 원시 LaTeX 0·유니코드 첨자 0, 수식 21개 정상 조판 |
| 5. 발표·시각 품질 | **충분** | 확대 제목 정상, 8개 상호작용 두 화면 PASS, 인쇄 외톨이 페이지·코드 분할 해소 |
| 6. 추적성과 출판 무결성 | **충분** | 검증 5종이 최종 바이트 이후 실행되어 전부 통과. provenance는 R2에서 커밋 고정 확인 완료 |

**여섯 게이트 모두 충분. 중대 0건 · 보완 0건 · 제안 1건.**

## 남은 결함

- **없음(보완 이상).** R2의 보완 3건이 모두 종결됐고 새로 발견한 결함이 없습니다.
- 제안 1건은 R2의 헤딩 레벨 건너뜀(h1 → h3)이며, 요청대로 **템플릿 전체의 접근성 설계 검토 대상으로 이월** 합니다. 이번 회차의 회귀가 아닙니다.
- 지켜볼 점 하나만 남깁니다. 인쇄 규칙 `pre { break-inside: avoid-page }` 가 공용 템플릿에 들어갔으므로, 앞으로 **한 페이지를 넘는 코드 블록** 이 있는 회차에서는 배치가 어떻게 되는지 한 번 확인하는 편이 좋습니다. 이번 두 블록은 모두 한 페이지에 들어갑니다.

## 발행 준비 판정

**발행 가능합니다.** 배포 전 로컬 상태 기준으로 여섯 게이트가 모두 충분하고, 막는 항목이 없습니다.

공개 페이지 확인(main 병합, Pages `headSha` 대조, 상태 `built`, 공개 딥링크의 데스크톱·모바일 실제 픽셀)은 이 검토에 포함되지 않았으며 Codex가 배포 뒤 수행합니다.
이 문서의 어떤 판정도 실서버 화면을 확인했다는 뜻이 아닙니다.

요청대로 브라우저·테스트를 직접 실행하지 않았고, `materials` 심링크를 포함해 이 검토 파일 외에는 아무것도 만들거나 고치지 않았습니다.
