# ML4T 3e Chapter 1 발표자료 품질 루프

대상: 2026-09-12, 발표자 태영. 출발점은 발행된 상세 리포트와 study HEAD `f8a6ebde992bed5b3d49e1675ee4b1b96938ec6b`. 사용자 요청은 상세 리포트의 의미를 읽기 쉬운 발표 화면으로 옮기고 기존 제작 스킬의 설명 공백을 보완하는 것이다.

## 스킬 기준선 검토

- [Codex 분석 및 지적 처리](2026-09-12-ml4t-ch01-deck-skill-analysis.md)
- [Claude 독립 분석](2026-09-12-ml4t-ch01-deck-skill-claude.md)

두 첫 분석은 서로의 판정을 읽기 전에 작성했다. Claude는 같은 Herdr workspace `w8`, 기존 `quant-claude` (`w8:p1`, 프로젝트 루트)에서 검토했다. 임시 탭은 만들지 않았다. 검토 전후 6개 기준선 파일 해시는 일치했다.

주요 결함은 설명의 존재를 추적성으로 대신하는 빈틈, 보고서만 먼저 발행했을 때 필수 그림 지정 누락, 코드에서 실제 출력으로 이어지는 검토의 부재, 서로 독립이어야 할 리포트·덱 캡션 번호의 충돌이다. 장수 상한도 설명 삭제를 유도할 수 있으나 장수를 늘린 사실만으로 이해도가 나아졌다고 판정하지 않았다. 고정 최소 그림 수와 캡션 span 개수로 설명성을 판단하는 제안은 기각하고, 원자료에 맞는 필수 지정과 본문 의미 검토를 채택했다.

수정 계층은 study-presentation 스킬, 제작 절차·청사진·덱 템플릿, validate-site.py와 기존 테스트다. 스킬 선택 description과 권한·출판 경계는 바꾸지 않았다. 회차 작성용 보조 코드는 owner scratch에만 두었다.

## 제작 중 자체 수정

처음 12장 표본을 실제 렌더한 결과 일부 그림 캡션이 flex 높이 계산으로 잘렸고 세로 도식이 작았다. 공용 그림 레이아웃에서 이미지와 캡션의 공간을 함께 배정하고 워크플로·연구 진입점·AI 작업 도식을 의미가 같은 가로 배치로 다시 그렸다. 모든 형제 그림을 다시 감사했다. 실습 입력 종목과 샤프 계산 의미는 17·18번 화면에 보강했다. 초기 캡처는 최종 증거로 사용하지 않았다.

## 덱 R1 · Codex 독립 판정

2026-09-06 14:44 KST. Claude의 덱 판정 문서는 아직 읽지 않은 상태다. 고정 대상은 owner 실행 기록의 `deck-r1-baseline.json` 64개 소스다. 동일 기준선으로 요청했으며 검토 중 변경하지 않는다.

| 게이트 | Codex | 근거 |
|---|---|---|
| 원본 구조 충실성 | 충분 | §1.1–1.6의 정확한 절 제목 책임 화면은 4·10·20·26·41·46. 제목 장만 두지 않고 다음 설명을 연속 배치. 원서 그림6개와 실습·진단 그림4개 전부 이월. |
| 의미와 주장 충실성 | 충분 | 31·32·35의 원서 보고값을 가이드 재실행과 구분. 35의 낙폭 집계, 39의 trace, 40의 종가 이후 소속 확률의 뜻과 한계 보존. 예시용 숫자는 예라고 표시. |
| 독립 문서 완결성 | 충분 | 2–3의 장 고유 질문, 11의 사용 가능 시점, 13의 IC·시간 평가, 28–29의 GMM 원리·기호, 46–47의 정리·질문이 본문에 있음. 노트만으로 본문 누락을 보충하지 않음. |
| 학습성과 설명성 | 충분 | 모든 그림은 읽을 관계·결과와 해석 경계가 같은 화면에 있음. 실습1은17→18–19, 실습2는37–38→39–40으로 입력·계산·실제 결과·한계가 이어짐. |
| 발표·시각 품질 | 충분 | 51장 전부1600×900·1366×768 렌더, 14장 모바일 표본. 코드17·38, 표12·18, 수식29, 긴 제목41, 그림8·10·21·24·35·37·40·49, 마무리51 등 원본 해상도로 점검. 14개 상호작용 항목 두 viewport 모두 PASS. 덱 PDF51페이지, 리포트A4 34페이지. |
| 추적성과 출판 무결성 | 충분(로컬) | 모든 refs·절 좌표·필수 그림·자산·번호 및 검토 해시 검사 통과. 전체 테스트284개 중279성공·선택 SDK5skip. 빌드 최신성·스킬 quick_validate·diff 검사 통과. 실제 배포 SHA와 공개 링크 확인은 출판 후 별도 기록한다. |

자동 검사는 설명의 의미 적합성을 입증하지 않는다. [교수 내용 대응 기록](2026-09-12-ml4t-ch01-deck-content.json)은 11개 설명 단위와 51장·근거 절·두 실험의 연결 및 현재 HTML 해시를 담는다.

### 증거와 한계

실제 캡처는 `tmp/browser-shots/ml4t-ch01-deck-r1-20260906/`에 있고, owner 실행 기록은 `/home/restful3/.local/state/codex/ml4t-ch01-slides-20260906/`에 있다. `QA_READY.md`, `final-render-hashes.json`, `per-slide-freshness.json`으로 증거의 범위와 신선도를 복원할 수 있다. desktop49장은 공유 HTML·CSS·JS·자산과 각 slide block이 동일함을 증명하고, 본문이 바뀐17·18장은 재캡처했다. projector·mobile·PDF는 최종 본문 이후 생성됐다. 캡처 묶음은52/52·71/71·4/4 완료이며 최종 실패0이다.

R1 리포트 변경은 실제로 그림의 required 속성뿐이었다. 초기 요청서와 이 원장은 Slides 링크도 추가됐다고 잘못 기록했다. 문자열 제거 뒤 바이트 동등성을 확인하는 검사는 애초에 링크가 없던 경우도 통과했고, Claude가 실제 anchor 부재를 확인하면서 이 기록 오류를 발견했다. R2에서 링크를 실제 추가하고 양방향 연결을 검사하는 조건을 보강했다. CPU 실습은 앞선 리포트 제작에서 얻은 2026-09-06 실제 출력이다. 이번 슬라이드 작업에서 실험을 다시 실행했다고 주장하지 않는다.

약49.6분의 발표 시간은 계획치이고 실제 리허설 시간은 아니다. 모바일의 고정16:9 슬라이드 바깥 여백은 정상이며 상세 독서는 리포트와 확대 보기를 지원한다. 브라우저 자체 전체화면 진입은 자동 상호작용 테스트에 포함하지 않았다. PDF는 검증용으로 생성했으며 별도 공개 산출물로 등록하지 않았다.

## Claude R1와 최종 처리

[Claude R1](2026-09-12-ml4t-ch01-deck-claude-r1.md)은 중대0·보완3·제안1로 판정했다. Codex와 이견이 있었던 게이트4·6의 지적은 실제 코드와 링크를 확인해 모두 채택했다. R1은 상호 충분이 아니다.

공용 템플릿도 실제 회차 생성기와 같은 순서로 deck assets와 report assets를 합친 미리보기에서 7장 전부1366×768로 렌더했다. 정의·도형·결과 표 대표 화면을 원본 해상도로 확인했다. `r1-template-combined.log`는7/7, 최종실패0이다. 템플릿 디렉터리 자체를 바로 여는 초기 검사는 보고서 공용 도형 복사 전이라 이미지가 없었으며 최종 증거에서 제외했다. 이는 생성기 결함이 아니어서 저장소 구조를 바꾸지 않았다.


## R2 수정과 재검증

| 지적 | 처리와 근거 |
|---|---|
| 두 실습에서 r이 서로 다른 형태인데 앞 셀로 지시 | 채택. 17번을 리포트의 returns·parts 이름으로 복원하고 발췌라고 표시했다. 38번에 종목별 수익률 표를 만드는 r 계산을 직접 넣고 앞 실습과 달리 평균하지 않는다고 설명했다. |
| 영문 곡선 범례와 탐색·확인의 연결 누락 | 채택. 리포트의 그림3 주석부터 파랑 development=탐색125일, 주황 locked_evaluation=확인126일을 설명하고 19번에도 이월했다. 차트나 수치는 바꾸지 않았다. |
| 리포트의 Slides 링크 누락 | 채택. 실제 상대 링크를 추가하고 report-teaching-v1의 짝 리포트에 덱 anchor가 있어야 통과하도록 검사와 회귀 사례를 보강했다. 실제 설정 메뉴를 열어 확인한다. |
| 청중용 마무리의 발표자 노트 링크 | 제안 채택. 51번에서는 링크를 제거했다. 발표자 노트 파일은 유지해 사용자에게 직접 전달한다. |
| 템플릿 공용 도식의 보조 글자 대비 | 자체 추가 발견. 세 형제 SVG에서 class의 fill이 밝은 presentation attribute를 덮는 문제를 inline fill로 고쳤다. 좌표·내용·색상 의도는 그대로이며 생성기 결합 미리보기와 각 도식의 실제 크기로 재렌더했다. 기존 회차 snapshot은 이 변경의 대상이 아니다. |

변경된 발표 화면은17·19·38·51이며 나머지47장과 공유 HTML/CSS/JS/회차 SVG가 그대로임을 `r2-freshness.json`으로 증명한다. R1 PNG는 보존하고 R2 산출물은 `tmp/browser-shots/ml4t-ch01-deck-r2-20260906/`에 따로 둔다. 리포트는 이번에 범례 문장이 추가됐으므로 새 A4를 생성한다. 해시와 두 검토 기록을 갱신한다.

### R2 Codex 재검증 판정

Claude R2 판정 수령 전 자체 검토: 게이트1·2·3·4·6은 충분(6은 로컬), 게이트5는 보완 필요. 17·19·38·51의 세 viewport, 보고서 범례 desktop/mobile 및 A412페이지, 전체51장 상호작용을 확인했다. 새 덱PDF51쪽·리포트A4 34쪽과 자동 검사도 정상이다.

새 Slides 링크로 메뉴가4개가 되자 `report-quick-links`의 무줄바꿈 배치에서 네 번째 회차 버튼이 오른쪽으로 돌출했다. 보고서 설정 desktop 실제 픽셀로 확인했고 R2의67개 기준선을 유지한 채 Claude에게 알렸다. 모바일 설정 초기 캡처는 is-open 상태이지만 CSS transition의 opacity가0에 머문 캡처여서 열림 증거로 인정하지 않았다. 클릭 결과와 전환 종료 후의 실제 배치를 분리해 검사한다. 이 문제는 보고서 본문이나 슬라이드 의미가 아닌 설정 메뉴 레이아웃에 한정된다.

## R3 · 설정 메뉴 종료 검증

[Claude R2](2026-09-12-ml4t-ch01-deck-claude-r2.md)는 R1 지적을 전부 해결로 인정했고 설정 메뉴의 돌출과 모바일 열림 증거만 보완으로 남겼다. 두 항목 모두 채택했다.

공용·회차 report.css의 `.report-quick-links`에 `flex-wrap: wrap`과 `min-width: 0`을 추가했다. 두 줄 이외 스타일이 동일하고 보고서·덱 HTML도 R2 그대로다. 설정 패널은 인쇄에서 숨겨지므로 R2 본문·PDF 증거를 재사용한다. 새 메뉴 캡처는 `tmp/browser-shots/ml4t-ch01-deck-r3-20260906/`에 있다. 실제 클릭에 따라 열린 패널의 CSS 전환을 finish()로 완료한 상태이며 스타일 값을 강제하지 않았다. 두 viewport 모두 OPEN=true·opacity1·네 링크 패널 내부 PASS이고, 실제 PNG에서 버튼·텍스트·테두리의 잘림·돌출이 없다.

Codex R3 판정(Claude R3 수령 전): 여섯 게이트 모두 충분, 출판은 로컬 기준. 게이트1–4·6은 변경 없는 R2 근거를 재사용했고 게이트5의 남은 메뉴를 직접 재검증했다. 전체284테스트 중279성공·5선택SDKskip, 교재 연결 사이트 검사 오류0, 빌드 최신성·diff 검사 통과. Claude R3 최종 판정은 수령 후 기록한다.

### 상호 충분 종료

[Claude R3 최종 기록](2026-09-12-ml4t-ch01-deck-claude-r3.md)은 고정68개 소스의 시작·종료 해시를 직접 확인하고 여섯 게이트 모두 충분, 중대0·보완0·제안0으로 판정했다. Codex도 동일 기준선에 전부 충분이다. R1·R2에서 발견한 내용·연결·메뉴 문제는 모두 수정·재검증되어 3라운드에서 로컬 품질 루프를 종료한다.

발행 단계는 이 변경 집합을 main에 커밋·push한 뒤 같은 head SHA의 Pages 작업 성공, 공개 HTML·자산48개 내용 해시, 공개 덱의 대표 화면 desktop/mobile을 확인한다. exact commit·작업URL·공개 검증 결과는 owner 실행 기록의 `PUBLISH_RESULT.md`와 `public-verification.json`에 남긴다. 기존 quant-claude 탭은 유지하며 임시 탭은 없었다.
