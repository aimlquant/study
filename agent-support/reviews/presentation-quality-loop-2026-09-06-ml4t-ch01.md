# ML4T 3e Chapter 1 상세 리포트 품질 기록

대상: `2026-09-12-ml4t-ch01`, 발표자 태영. 사용자가 요청한 범위는 스킬 개선과 상세 리포트 발행이다. 슬라이드는 이번 산출물에 포함하지 않는다. 기존 공개 저장소 기준 커밋은 `ffc7c90`이며, 시작 시 작업 트리는 깨끗했다.

## 라운드 1 — Codex 독립 검토

Claude의 첫 판정이나 원장을 읽기 전에 작성했다. 기준선은 2026-09-06 12:56 KST 작업 트리이며, 파일별 SHA-256은 소유자 로컬 작업 기록 `ml4t-ch01-report-20260906/review-baseline-r1.json`에 고정했다. Claude는 같은 기준선으로 검토하며 별도 검토 문서만 소유한다.

| 게이트 | 판정 | 증거와 남은 확인 |
|---|---|---|
| 원본 구조 충실성 | 충분 | 원서의 6개 절과 6개 그림 좌표, 절 내부 소제목을 본문에 통합했다. 요약 수치로 다시 그린 국면 그림은 생략한 시간축 범위를 명시했다. |
| 의미와 주장 충실성 | 보완 필요 | 책 보고값과 교육용 실행값을 분리했다. 본문의 일부 인라인 수식에 원시 `\mid` 문자가 남아 있다. 도식의 연결선 오류는 아래 항목대로 수정해야 한다. |
| 독립 문서 완결성 | 충분 | 시간 경계·GMM·국면 해석·기관과 독립 연구자의 차이를 입력, 이유, 결과, 한계 순서로 설명한다. |
| 학습성과 설명성 | 충분 | 실제 실행 노트북의 코드·표·차트·해석을 함께 제시한다. 연환산 산술평균과 CAGR, 동시점 국면 분류와 미래 예측을 구분한다. |
| 발표·시각 품질 | 보완 필요 | 모바일 도해 가로 잘림, 연결선과 라벨 충돌, 일부 A4 끝 페이지의 큰 여백을 확인했다. 전체 PDF 41쪽의 상세 검수는 계속한다. |
| 추적성과 출판 무결성 | 보완 필요 | 테스트 280개 중 275 통과·5 건너뜀, 사이트 검증 통과. 최종 렌더, 생성 페이지 공백 검사, 배포 검증은 남아 있다. |

### 관찰한 결함과 조치 대상

1. **보완 / 회차 CSS**: 390px 화면에서 `.figure-scroll img { min-width: 590px; }`가 도식을 잘라 보이게 한다. 전체 관계를 먼저 볼 수 있게 바꾸고 확대 기능을 확인한다.
2. **보완 / lifecycle.svg**: 오른쪽 피드백선이 `갱신` 라벨을 통과한다. 라벨과 선의 위치를 분리한다. 같은 종류의 모든 도해를 함께 검사한다.
3. **보완 / workflow.svg**: 연구 프레임워크에서 아래로 향하는 화살표가 특징·모델 상자 사이의 빈 공간에 도착한다. 목표 상자의 올바른 경계에 연결한다.
4. **보완 / 본문**: 인라인 수식의 원시 마크업을 읽을 수 있는 수식 또는 일반 기호로 고친다.
5. **보완 / report.js QA**: `fragment-capture`가 중첩된 실습·소제목 대신 상위 절의 시작을 캡처한다. 23개 파일 생성 성공을 해당 실습의 화면 검증으로 간주하지 않는다. 정확한 위치를 다시 캡처한다.
6. **보완 / A4**: 16·21·37쪽 등에서 소량의 문장만 남는 페이지를 확인했다. 제목과 본문 결합을 유지하면서 불필요한 여백을 줄인다.
7. **보완 / 생성 페이지**: 새 회차 요약에 논의 항목이 없을 때 생성기에 빈 공백 줄이 남아 `git diff --check`가 실패한다. 생성 로직에서 해결한다.
8. **보완 / A4 코드**: 29쪽의 GMM 코드에서 긴 행이 인쇄 폭 밖으로 잘리고 스크롤바가 남는다. 코드 행을 읽기 좋게 나누고 인쇄 스타일에서 줄바꿈과 전체 표시를 보장한다.

### 기준선 검증

- 교재 심링크 연결 후 전체 테스트: 280개, 275 통과, 5 건너뜀.
- `quick_validate.py`: study-presentation 스킬 통과.
- 사이트 검증: 95개 HTML, 오류 0. 기존 공개 YouTube iframe 경고 22개.
- 교육용 노트북: 복사본의 22개 셀을 CPU로 실행, 오류 0. 원본 노트북은 변경하지 않았다.
- 보호된 브라우저 직렬 렌더: PNG 22개와 A4 PDF 1개 생성 성공. 중첩 fragment의 정확한 위치는 위 결함 때문에 아직 미검증이다.
- A4: 41쪽, 전체 연락판 및 선택한 원본 크기 페이지를 검사했다. 연락판은 가독성 증거로 취급하지 않는다.

Claude 검토를 받은 뒤 지적별 채택 여부와 다음 라운드 결과를 이 문서에 이어 기록한다.

## 라운드 1 — 결과 대조와 반영

[Claude R1](2026-09-12-ml4t-ch01-claude-r1.md)을 자체 첫 판정 뒤에 읽었다. 원장 첫 요약은 보완 5건으로 적었지만 실제 번호는 보완 1–6이며, 아래에서는 6건 모두 처리한다.

| 지적 | 결정과 근거 | 수정 위치 |
|---|---|---|
| Claude 보완 1: 생애주기 단계 과업 생략 | 채택. 6개 단계의 범위 설정, 준비, 학습, 평가, 통합·폴백, 감시 과업을 그림 바로 다음 문단에 보충했다. | report.html §1.1 |
| 보완 2·3: 수식 구분자·첨자 | 채택. 인라인 KaTeX 구분자와 성분·표준화 기호를 일관되게 복구하고 정규밀도 기호와 합산 첨자를 정의했다. | report.html §1.1·1.4 |
| 보완 4: 중첩 fragment 중복 캡처 | 채택. QA 전용 모드에서 목표부터 보고서 루트까지 앞 형제 노드를 제거하도록 고쳤다. 일반 독자 앵커는 그대로다. | 공용·회차 report.js |
| 보완 5: 교재가 미커밋 상태라는 판단 | 사실 판단은 기각. 직접 git show로 대조한 원자료 10개 모두 d78ed12af8219f21495b1fd7ae2ec601d73cba7f에 이미 포함돼 있고 JSON의 해시와 일치한다. 관련 교재 파일은 수정 상태가 아니다. 재현성을 명시하자는 제안은 채택해 revision을 기록했다. | content.json·제작 절차 |
| 보완 6: §1.4 상단 캡처 누락 | 채택. 여섯 절, 실습·GMM 해설, 모든 그림을 데스크톱과 모바일 매니페스트에 넣었다. | 로컬 렌더 매니페스트 |
| 제안 1: ML 역할 행의 오독 | 채택. 드리프트 탐지가 원서 표의 실행·비용 행에 있으나 감시에도 쓰임을 도해 메모에 설명했다. | fig-ai-workflow 메모 |
| 제안 2: 셀 해시 노출 | 채택. 독자 화면에는 셀의 이름을 쓰고 exact ID는 제작용 실습 근거 기록에 유지했다. | report.html·content.json |
| 제안 3: 전환 빈도 개념도 | 미채택. 실제 월별 경로를 확보하지 않은 개념 시간띠는 관측 경로로 혼동될 수 있다. 267회·약 4개월은 본문에서 해석하고 원래 시간축 생략 범위는 그림 옆에 명시했다. | 기존 해석 유지 |
| 제안 4: 실행 차트 한글 축 | 미채택. 노트북의 실제 출력과 일치하는 차트를 유지하고 축·확률·단위·기간은 한국어 캡션에서 풀었다. | 기존 출력 유지 |
| 제안 5: 교재 검사 옵션 | 채택. 교재가 로컬에 없으면 실패시키는 옵션임을 절차에 설명했다. | 제작 절차 |

범용 LaTeX 제어열 금지와 바이트 동일 이미지 일괄 실패 제안은 자동 게이트로 추가하지 않았다. HTML에는 렌더 전의 정상 TeX와 설명용 코드가 존재하며, 서로 다른 앵커가 같은 화면을 가리키는 합법적인 경우도 있다. 이번에는 실제 수식 렌더·PDF 추출과 목표별 화면 대조로 검증한다.

자체 지적도 반영했다. 모바일 도식은 전체가 먼저 보이도록 가로 최소 폭을 없앴다. 생애주기 라벨과 피드백선을 분리하고 워크플로 화살표를 실제 상자 경계로 연결했다. 긴 코드의 A4 줄바꿈과 Python 표지를 공용 템플릿·회차에 함께 적용했다. 회차 인쇄 본문은 15px·행간 1.7로 조정했다. 회차 요약 생성기의 공백과 장 안내/운영 논의 구분을 고치고 회귀 테스트를 추가했다.

추가 상호작용 감사에서 그림 래퍼 때문에 이미지 확대 이벤트가 연결되지 않은 점을 확인했다. 공용·회차 JS 선택자에 figure-scroll 아래 이미지를 포함하고, h3 실습 제목을 목차에 넣었다. 로컬 브라우저 테스트 페이지는 목차 이동, 모든 그림의 키보드 접근, Enter·클릭 확대, 확대 배율, Esc 닫기와 포커스 복귀를 검증한다. 이 테스트 페이지는 공개 산출물에 포함하지 않는다.

스킬 개선은 이 작업에서 확인한 절차·검증 누락에 한 번만 적용했다. 스킬의 자동 선택 범위나 공개 권한 경계를 넓히지 않았다.

## 라운드 2 — Codex 재검증

기준선: 소유자 로컬 review-baseline-r2.json. Claude R2 판정을 읽기 전에 작성한다.

- 원본 구조 / 의미·주장 / 독립 완결성 / 학습 설명성 / 출판 추적성: **충분(발행 전)**. 원자료 10개의 커밋 바이트와 해시, 모든 좌표, 실행 수치, 282개 테스트(277 통과·5 skip), 사이트 검증과 생성 최신성에 문제가 없다.
- 화면 품질: 데스크톱·모바일 도식 전체 표시, 연결선, 두 실습의 실제 목표 화면, 목차·확대·키보드 상호작용이 정상이다. 모든 그림의 실제 렌더와 표·코드·수식을 확인했다.
- A4는 **보완 필요**. 37쪽이고 21개 수식의 원시 구분자 누출은 없다. 코드의 가로 잘림은 해결했다. 다만 3쪽에는 소개의 마지막 문단만, 8·20쪽에는 출처 문장만 남으며, GMM 코드가 27·28쪽 사이에서 갈라진다. 본문 웹용 절 위아래 80px 여백이 인쇄에도 적용되는 것이 원인이다. 절의 새 페이지 시작은 유지하되 인쇄 전용 여백을 줄이고 짧은 코드 블록을 묶는 후속 보완이 필요하다.
- R2 최종 이미지·PDF 51개는 현재 소스의 마지막 수정 시각 이후 생성됐다. source-hashes.json, qa-ready.json에 소스와 출력 SHA-256·URL을 기록했다. 중간 실패 PDF나 처음 생성한 파일은 최종 증거로 사용하지 않는다.

A4 보완 후 해당 인쇄 계층만 재검증한다. 현재 Claude 검토 대상은 고정한 채 판정을 기다린다.

## 라운드 3 — 마지막 표시 수정과 Codex 판정

[Claude R2](2026-09-12-ml4t-ch01-claude-r2.md)의 보완 3건을 모두 채택했다. 확대 제목은 칩과 제목을 개별 선택해 “그림 2 · 공유 데이터 기반과 탐색·확인의 경계”로 표시한다. 공용·회차 JS 두 곳을 함께 수정했다. R2의 인쇄 여백과 코드 묶음 제안도 공용·회차 인쇄 CSS에서 처리했다. h1→h3 계층 개선 제안은 기존 공용 템플릿의 구조 계약과 접근성 설계를 함께 바꿀 사안으로 남긴다. 이번 변경에 필요한 목차·키보드 탐색은 실제 브라우저에서 확인됐다.

R2→R3의 변경 파일은 report.css 두 개와 report.js 두 개뿐이다. 리포트 HTML, 그림 10개, 원자료·실습과 내용 검토 JSON의 해시는 R2 그대로다. CSS는 인쇄 전용 규칙 추가, JS는 확대 보기 이름 생성 함수 변경뿐이므로 R2의 본문·도식 화면 증거는 해당 표시 범위에 그대로 유효하다. 변경된 인쇄와 확대 보기 화면은 새로 렌더했다.

최종 소스 수정 뒤 필수 검사를 다시 실행했다.

- 전체 테스트 282개: 277 통과, 5 건너뜀. 건너뛴 항목은 설치되지 않은 httpx·neo4j·OpenAI SDK의 기존 오류 재시도 검사다. 교재 대조 검사는 연결된 실제 교재로 실행됐다.
- 사이트 생성: 최신. build_site.py --check: 최신.
- 사이트 검증: 95 HTML, 오류 0, 기존 외부 iframe 경고 22개.
- git diff --check: 통과. skill-creator quick_validate: 통과.
- 실제 확대 보기: 데스크톱·모바일 8개 상호작용 PASS, 분리된 그림 번호·제목을 육안 확인.
- A4: 34쪽, 수식 21개 원시 구분자 누출 0. 도입의 마지막 문단과 절 말미 출처만 남던 페이지가 정리됐다. §1.2 도입과 워크플로 그림은 같은 페이지에서 읽힌다. GMM의 전체 코드는 24쪽에 모여 가로 잘림이나 페이지 사이 분할이 없다.
- 절 새 페이지 시작은 유지했다. 절 끝의 해석·한계와 출처가 함께 놓인 짧은 페이지, 용어·참고문헌 페이지의 여백은 내용 누락이나 고립된 출처가 아니므로 허용한다.

Codex 최종 판정(Claude R3 결과를 읽기 전): 원본 구조·의미/주장·독립 완결성·학습 설명성·발표/시각·발행 전 추적성 모두 **충분**. 중대·보완 결함 0. 실서버 확인은 배포 뒤 별도 수행한다.

## 종료 판정

[Claude R3 최종 판정](2026-09-12-ml4t-ch01-claude-r3.md)에서 여섯 게이트 모두 충분, 중대·보완 0건을 확인했다. Codex 판정과 일치하므로 **3라운드에서 상호 충분으로 종료**한다. 확대 제목, 인쇄, 최종 바이트 검증의 세 잔여 항목은 모두 닫혔다. h1→h3 접근성 계층은 공용 템플릿 전체를 검토할 향후 제안이며 이번 회차의 회귀나 발행 차단 항목이 아니다.

발행 대상은 2026-09-12 회차의 상세 리포트 1개이며 발표자는 태영이다. 회차의 stable ID와 공개 URL을 유지했다. 배포 전 검증과 실서버 확인을 구분하며, 배포 후 main 커밋·Pages head SHA·공개 파일 해시·실제 화면 확인 결과는 소유자 로컬 ml4t-ch01-report-20260906/PUBLISH_RESULT.md에 기록한다. 이 원장의 발행 전 판정을 실서버 검증으로 대신하지 않는다.
