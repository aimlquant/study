__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
.env
.env.*
*.token.json
*credentials*.json
*client_secret*.json
*.sqlite
*.sqlite3
*.db
tmp/
.DS_Store
._*
# Recordings and generated media stay in the private operations repository.
*.mp4
*.mkv
*.mov
*.m4a
*.wav

# 교재는 aimlquant/study-materials (private) 에서 관리한다.
# 뒤에 슬래시를 붙이면 디렉터리만 매칭해 검증용 심링크가 새어나온다.
/materials

# 로컬 작업 인계 메모는 공개 저장소에 포함하지 않는다.
/HANDOFF.md
